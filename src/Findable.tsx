export const Findable = () => {
  return (
    <div>
      <h2 className="text-lg">Findable</h2>
      <p>
        1. Are you aware that a data(set) should be assigned a globally unique
        persistent and resolvable identifier when deposited with a data
        repository?{" "}
      </p>
    </div>
  );
};
